- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart hcultmon
  ansible.builtin.systemd:
    name: hcultmon
    state: restarted

- name: restart hcultctrl
  ansible.builtin.systemd:
    name: hcultctrl
    state: restarted
